<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:simpleCore="clr-namespace:SimpleToolkit.Core;assembly=SimpleToolkit.Core"
    xmlns:pages="clr-namespace:Playground.Original.Views.Pages"
    x:Class="Playground.Original.Views.Pages.ImagesPage"
    x:Name="page"
    Title="ImagesPage">

    <ScrollView>
        <simpleCore:WaterfallGrid
            x:Name="waterfall"
            Background="LightBlue"
            Padding="20, 10"
            AlignContent="Start"
            VerticalOptions="Start"
            Span="2" RowSpacing="8" ColumnSpacing="8"
            BindableLayout.ItemsSource="{Binding Source={x:Reference page}, Path='Images'}">
            <BindableLayout.ItemTemplate>
                <DataTemplate x:DataType="pages:ImageModel">
                    <Image Source="{Binding Url}" Aspect="AspectFill"/>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </simpleCore:WaterfallGrid>
    </ScrollView>
</ContentPage>
